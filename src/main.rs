fn main() {
    // This main function is typically just a placeholder for benchmark projects.
    // The actual benchmark code runs via `cargo bench`.
    println!("Run `cargo bench` to execute the serialization benchmarks.");
}
